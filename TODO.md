# Todo

- [ ] Add reference image functionality to Step 1 — allow users to upload or pick a reference image that gets passed to Gemini during generation (Go Direct and Brainstorm → Generate). Requires frontend state (thumbnail + clear button in ThemeInput) and backend changes to `/api/generate` to accept and forward the reference image to the model.
